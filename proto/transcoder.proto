syntax = "proto3";
package autoripper;
import "common/path.proto";
import "common/empty.proto";

service Transcoder {
  rpc BeginTranscoding (autoripper.common.Path) returns (autoripper.common.Empty) {}
}
